{
  "name": "browser-automation-agent",
  "version": "1.0.0",
  "type": "module",
  "description": "Browser automation using Isaac vision model and GPT-5.2 reasoning model",
  "license": "MIT",
  "scripts": {
    "build": "node build.cjs",
    "watch": "node build.cjs --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.0",
    "eslint": "^9.39.2",
    "prettier": "^3.8.1",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.53.1"
  },
  "dependencies": {
    "preact": "^10.28.2"
  }
}
